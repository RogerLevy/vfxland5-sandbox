trait: %micro-like-actor
    \ workaround of a flaw in NIBS that prevents traits with already-supported super-traits 
    \ from being inherited
    \ this is fine so long as we don't actually need to check if an object is a %micro-like
    \ TODO: fix APPLY-TRAIT and INHERIT-TRAIT 

    \ is-a %element
    :: clickbox@ ( - x1 y1 x2 y2 ) $DEADBEEF dup dup dup ;
    :: action ;
    \ prop en          \ user disable switch
    prop val :fixed  \ coefficient, must be >= 0.01 to process
    prop dcy :fixed  \ scalar
    static icon-color \ rgba8
trait;

class: %actor
    \ allow actors to behave as micros

    is-a %micro-like-actor
    template { en on 1.0 val ! 0.97 dcy ! -1 icon-color ! }
    :: clickbox@ x 2@ x1 2@ 2-`` w1 2@`` box ;
class;
